rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Enable seamless real-time database state synchronization across all devices
    match /appState/{document=**} {
      allow read, write: if true;
    }
  }
}
